const millisecond = 1000;
const second = 5;

const config = {
	idLength: 16,
	tickerDelay: millisecond * second,
	maxTaskCount: 1,
	maxTaskLength: 10,
	themes: [
		'light', 'dark',
	],
};

export default config;
